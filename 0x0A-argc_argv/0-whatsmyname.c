#include <stdio.h>
#include "main.h"

/**
 * main - print the name of the program "0-whatsmyname.c"
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("argc = %d\n", *argc);

	return (0);
}
